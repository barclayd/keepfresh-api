[package]
name = "keepfresh-api"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.7"
dotenv = "0.15.0"
serde = { version = "1.0.215", features = ["derive"] }
tokio = { version = "1", features = ["full"] }
